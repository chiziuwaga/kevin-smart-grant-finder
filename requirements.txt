# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.23.2
gunicorn==20.1.0

# Database (Postgres-native, no external vector DB needed)
SQLAlchemy==2.0.41
alembic==1.16.1
psycopg2-binary==2.9.10
asyncpg==0.30.0

# Authentication (JWT - simple email/password auth)
python-jose[cryptography]==3.3.0
python-multipart==0.0.7
bcrypt==4.2.1

# Payments (Stripe - not yet configured, will be added by owner)
stripe==7.8.0

# Email (Resend)
resend==0.8.0

# Background Tasks (Celery)
celery[redis]==5.3.4
redis==4.5.4  # Compatible with celery 5.3.4 (requires <5.0.0)

# AI Integration
agentql==0.3.0  # Web scraping for grant discovery

# API Integration
aiohttp==3.9.3
python-dotenv==1.0.0
httpx==0.26.0  # For DeepSeek API + async HTTP requests
requests==2.31.0

# Data Processing
pydantic==2.11.5
pydantic-settings==2.7.1
python-dateutil==2.8.2
numpy==1.26.4
typing_extensions>=4.12.2

# Utilities
PyYAML==6.0.2
slowapi==0.1.9  # Rate limiting
bleach==6.1.0  # HTML sanitization

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0
pytest-mock==3.12.0
