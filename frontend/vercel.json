{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "https://smartgrantfinder.herokuapp.com/api/$1" },
    { "src": "/.*",        "dest": "/index.html" }
  ]
} 